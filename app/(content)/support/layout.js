import React from 'react'

const SupportLayout = ({children}) => {
    return (
        <div>
            <h1>This is Support Page Layout</h1>
            {children}
        </div>
    )
}

export default SupportLayout